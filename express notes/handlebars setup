npm install --save express-handlebars

var app = express();
var handlebars == require('express-handlebars');
handlebars.create({ defaultLayout: 'main' });
app.engine('handlebars', handlebars.engine);
app.set('view engine', 'handlebars');


Example usage:

<!doctype html>
<html>
<head>
	<title>something</title>
</head>
<body>
	{{{body}}}
</body>
</html>

Save in some location (views/layouts/main.handlebars)
Have corresponding views that determine actual pages

EG: views/home.handlebars
	<h1> Welcome! </h1>

To use these templates?

app.get('/', function(req, res) {
	res.render('home');
});

To render more interesting and dynamic things, eg,

views/about.handlebars

<blockquote>{{fortune}}</blockquote>

app.get('/about', function(req, res) {
	var randomFortune = fortunes[i];
	res.render('about', { fortune: randomFortune });
})